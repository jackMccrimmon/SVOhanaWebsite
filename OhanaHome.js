$(document).ready(function() {
    $("#slider").bxSlider({
        auto: true, //Makes slide show automatic
        pause: 4000, //4 second pauses
        pager: false, //hides the pager
        controls: false, //hides the controls
    });

});